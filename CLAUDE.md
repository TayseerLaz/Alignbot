# ALIGNED Business Platform — CLAUDE.md

> **For next-day Claude:** Read the `## Current Status` section first, then check `## Resume Checklist` at the bottom. The full 4-day plan is the source of truth for scope — do not re-design; execute what's here.

---

## 1. Project Context

**What:** Multi-tenant SaaS where ALIGNED's clients manage product/service catalogs, FAQs, business info. ALIGNED's WhatsApp chatbots read from this platform via a cached, low-latency API.

**Phase 1 goal (this plan):** Ship the Data Management Platform end-to-end in **4 calendar days** of Claude-assisted development.

**Full spec:** See the project details doc the user pasted on 2026-04-19 (Phase 1 §3, NFRs §6, Architecture Appendix). Phase 2 (AI bot builder) is out of scope for these 4 days.

---

## 2. Locked Decisions (do not re-litigate)

| Area | Choice |
|---|---|
| Backend | Node.js 22 + **Fastify** + TypeScript |
| ORM | **Prisma** (migrations + type-safety); raw SQL for RLS policies |
| DB | **PostgreSQL 16** + **PgBouncer** (transaction pooling) |
| Cache / Queue | **Redis 7** + **BullMQ** |
| Frontend | **Next.js 15** (App Router) + Tailwind + **shadcn/ui** + **IBM Plex Sans** |
| Auth | In-house (jose JWT access + httpOnly refresh cookies + bcrypt). RBAC: ALIGNED admin / Client admin / Editor / Viewer |
| Storage | **Wasabi** (S3-compatible) via AWS SDK |
| WhatsApp | **Meta Cloud API** (official, not WBiztool) |
| Email | **Resend** |
| Hosting | **Aligned Cloud Servers** via Docker Compose + Caddy (auto TLS) |
| CI/CD | **GitHub Actions** |
| Observability | Pino + Sentry + Prometheus /metrics endpoint |
| Multi-tenancy | Shared schema, `organization_id` column on every tenant-scoped table, **Postgres RLS** enforced as backstop, middleware sets `app.current_org_id` per request |
| Brand colors | Mediterranean Blue primary; IBM Plex Sans |
| Package manager | **pnpm** workspaces + **Turborepo** |

---

## 3. Repo Structure

```
Alignbot/
├── apps/
│   ├── api/          # Fastify REST API
│   ├── worker/       # BullMQ workers (imports, syncs, webhooks)
│   └── web/          # Next.js 15 client portal + ALIGNED admin
├── packages/
│   ├── db/           # Prisma schema, migrations, seed, RLS SQL
│   ├── shared/       # Zod schemas, types, constants (used by api + web)
│   └── config/       # ESLint, tsconfig bases, Prettier
├── infra/
│   ├── caddy/        # Caddyfile for TLS + reverse proxy
│   ├── pgbouncer/    # pgbouncer.ini
│   └── scripts/      # deploy.sh, backup.sh, load-test.sh
├── .github/workflows/
│   ├── ci.yml        # lint + typecheck + test on PR
│   └── deploy.yml    # build + push + SSH deploy on main
├── docker-compose.yml        # dev
├── docker-compose.prod.yml   # prod
├── pnpm-workspace.yaml
├── turbo.json
├── tsconfig.base.json
├── .env.example
└── CLAUDE.md         # this file — always update Status at end of session
```

---

## 4. The 4-Day Plan

Each day is roughly 8–12 hours of Claude-assisted work. Each day ends with something demoable.

### Day 1 — Foundation, Auth, Portal Shell ✅ COMPLETE (2026-04-19)

**End-of-day demo:** User can sign up, verify email, log in, see an empty dashboard styled in ALIGNED brand, invite a teammate with a role.

- [x] **1.1** pnpm + Turbo monorepo scaffold (root package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json)
- [x] **1.2** Shared tooling in `packages/config` (ESLint flat config, Prettier, tsconfig bases)
- [x] **1.3** `packages/db`: Prisma init, schema v0 (Organization, User, Membership, Role enum, Session, Invitation, AuditLog, ApiKey), initial migration
- [x] **1.4** RLS policies SQL (applied after Prisma migrations) — every tenant-scoped table enforces `organization_id = current_setting('app.current_org_id')::uuid`
- [x] **1.5** `packages/shared`: Zod schemas mirroring Prisma models, shared enums, API response envelope types
- [x] **1.6** `apps/api` Fastify skeleton: pino logger, error handler, Zod validation via fastify-type-provider-zod, `@fastify/swagger` OpenAPI, `@fastify/rate-limit`, `@fastify/cookie`, `@fastify/helmet`, `/health` and `/metrics` routes
- [x] **1.7** Tenant-context plugin: extract `org_id` from JWT, run `SET LOCAL app.current_org_id` on a Prisma transaction per request
- [x] **1.8** Auth routes: signup, login, logout, refresh, forgot-password, reset-password, verify-email, invites (create + accept + revoke), session, switch-org
- [x] **1.9** RBAC guard factory: `requireRole('admin' | 'editor' | 'viewer')` + `requireAlignedAdmin`
- [x] **1.10** Resend email templates: verify, reset, invite (Mailhog fallback in dev)
- [x] **1.11** `apps/web` Next.js 15 App Router scaffold: Tailwind v4, shadcn-style UI primitives, IBM Plex Sans via `next/font`, brand tokens
- [x] **1.12** Auth pages: `/signup`, `/login`, `/forgot-password`, `/reset-password`, `/verify-email`, `/invite/[token]`
- [x] **1.13** App shell: responsive sidebar nav (desktop + mobile drawer), top bar with user menu + org switcher, page header
- [x] **1.14** Dashboard page (empty state with counts + getting-started checklist + system status)
- [x] **1.15** Members page: list members, invite modal, role change (live select), deactivate, list + revoke pending invitations
- [x] **1.16** `docker-compose.yml` for dev: postgres, redis, pgbouncer, mailhog (dev email capture)
- [x] **1.17** Seed script: creates one demo org + admin user for quick iteration

### Day 2 — Catalogs + Business Info + Image Uploads ✅ COMPLETE (2026-04-20)

**End-of-day demo:** User can create, edit, search, and delete products (with variants + images), services (with pricing tiers + availability), and business info (hours, FAQs, policies).

- [x] **2.1** Prisma schema extension: `Product`, `ProductVariant`, `ProductImage`, `Category` (self-referencing tree), `Service`, `ServicePricingTier`, `AvailabilityWindow`, `BusinessInfo`, `Location`, `ContactChannel`, `FAQ`, `Policy`, plus `Asset`. pg_trgm GIN indexes + auto-maintained `search_text` triggers (raw SQL). All tables have `organization_id` + RLS.
- [x] **2.2** Wasabi storage module (`apps/api/src/lib/storage.ts`): S3 client, presigned PUT URLs (browser → Wasabi direct), `Asset` table records metadata. Upload path is two-step (presign → PUT → finalize). Returns 503 if Wasabi keys missing so dev still boots.
- [x] **2.3** Product API: `GET /products` (search via name/SKU/search_text, filter by category/availability/price range, cursor pagination), `POST`, `GET /:id`, `PATCH`, soft-`DELETE`, `POST /:id/images`, `DELETE /:id/images/:imageId`, `POST /:id/images/reorder`, `PUT /:id/variants` (replace-set), bulk `POST /products/bulk-update`.
- [x] **2.4** Service API: full CRUD + `PUT /:id/pricing-tiers` (replace-set) + `PUT /:id/availability` (replace-set, weekly grid).
- [x] **2.5** Business Info API: upsert `business-info`, full CRUD on locations / contacts / FAQs (with reorder) / policies (upsert by kind).
- [x] **2.6** Category API: CRUD + reorder. Self-parent guard. Deleting clears products' categoryId via `onDelete: SetNull`.
- [x] **2.7** Product list page: search (debounced), category + availability filters, bulk select with mark-available/unavailable, primary image thumbnail, status badges, dropdown actions.
- [x] **2.8** Product edit page: details with debounced auto-save (800 ms), image uploader (presign → upload → finalize → attach), variant editor with dynamic option keys, status + danger zone cards.
- [x] **2.9** Service list + edit pages: details auto-save, pricing-tier builder (with features list), weekly availability grid (open + start/end per day).
- [x] **2.10** Business info editor: tabbed page — Profile + hours grid, Locations (list + dialog), Contact channels (inline form), FAQs (inline create + visibility toggle + delete), Policies (upsert by kind).
- [x] **2.11** Activity log entries written via `recordAudit()` calls from each route on create/update/delete (product, service, category, faq, policy, business_info, asset_uploaded). Prisma `$use` middleware was avoided in favour of explicit calls for clearer intent.

### Day 3 — CSV Import + API Connectors + Read API ✅ COMPLETE (2026-04-21)

**End-of-day demo:** User uploads a product CSV → watches progress → sees imported products. Chatbot (simulated with curl) queries the read API and gets cached <100ms responses. Outbound webhook fires on change.

- [x] **3.1** BullMQ setup. `apps/api/src/lib/queues.ts` exposes `import`, `sync`, `webhook-delivery`, `email` queues; `apps/worker/src/index.ts` boots three workers (import / sync / webhook-delivery). Email queue is plumbed but no email worker yet (sendEmail still calls Resend/Mailhog directly from auth.service for Day 1 flows).
- [x] **3.2** Import templates — XLSX generator at `GET /api/v1/imports/templates/:kind` with a help sheet listing every field + required/optional + description. Targets: product, service, faq, business_info.
- [x] **3.3** Multipart upload endpoint at `POST /api/v1/assets/upload-csv` — server-side stream to Wasabi, creates an Asset row, returns assetId. `POST /api/v1/imports` accepts the assetId, creates an ImportJob, enqueues BullMQ.
- [x] **3.4** Streaming import worker (`apps/worker/src/jobs/import.ts`) — csv-parse for CSV, ExcelJS streaming for XLSX. Per-row Zod validate + upsert via shared `upsertOne()`. Writes ImportJobRow on success/failure with raw data + error list. Updates job progress every 25 rows. Honors cancellation between batches.
- [x] **3.5** Column mapping — supported in the API (`columnMapping` body field) and stored on ImportJob; UI for now defaults to "header name === field name" (good enough for our generated templates). Custom mapping wizard step deferred to Day 4 polish.
- [x] **3.6** Import status: list page with live polling, detail page with progress bar + per-row results + cancel button + filter to failed-only.
- [x] **3.7** `ApiConnector` + `SyncRun` models with auth config (bearer/api_key/basic/hmac), cron schedule, and webhook secret for inbound push.
- [x] **3.8** Connector UI: list + create dialog with auth-kind selector that swaps the credential fields, test-connection button, copy-webhook-URL action.
- [x] **3.9** Scheduled sync — BullMQ repeatable jobs registered in `connector.routes.ts` on create/update; sync worker pulls JSON, applies mapping, validates with shared upsert, records SyncRun.
- [x] **3.10** Inbound webhook receiver at `POST /api/v1/webhooks/inbound/:connectorId` — HMAC-SHA256 over `<timestamp>.<body>` with the connector's `webhookSecret`. 5-minute timestamp skew window. Creates a SyncRun and enqueues a sync job.
- [x] **3.11** Sync run history rendered inline under each connector in the UI (last 50, polls every 5s).
- [x] **3.12** Chatbot Read API at `/api/v1/read/*` (`/products`, `/products/:id`, `/services`, `/services/:id`, `/business-info`, `/faqs`, `/policies`, `/search`). API-key authed via `X-Aligned-Api-Key`. Scopes enforced per endpoint.
- [x] **3.13** Redis cache (`lib/read-cache.ts`) — keys `read:{orgId}:{endpoint}:{queryHash}`, 60s fresh / 5min stale TTL. SCAN-based pattern delete (no KEYS).
- [x] **3.14** Cache invalidation hooked into `emitWebhookEvent()` so every product/service/business-info/faq/policy create/update/delete clears `read:{orgId}:*` for the org.
- [x] **3.15** Outbound webhook system: WebhookEndpoint + WebhookDelivery models, full CRUD, HMAC-SHA256 signing, BullMQ delivery worker with exponential backoff (8 attempts), permanent-fail short-circuit on 4xx, auto-disable endpoint after 25 consecutive failures, manual retry endpoint.
- [x] **3.16** Per-key rate limit on read API (Redis-backed via `@fastify/rate-limit` keyGenerator), per-IP on portal endpoints. Configurable via `RATE_LIMIT_*` env vars.
- [x] **3.17** Filtered `/docs/chatbot` Swagger UI registered alongside the main `/docs`. Tags now categorise routes (auth, catalog, business-info, imports, connectors, webhooks, api-keys, chatbot-read).

### Day 4 — Versioning + Admin Panel + Hardening + Deploy ✅ COMPLETE (2026-04-22)

**End-of-day demo:** Deployed to Aligned Cloud Servers at a staging subdomain, TLS live, p95 <200ms on seeded data under load, rollback button works, ALIGNED admin sees all tenants.

- [x] **4.1** Versioning: `CatalogRevision` (entity_type, entity_id, organization_id, snapshot JSONB, version_number) + `recordRevision()` helper called from product/service/business-info CRUD.
- [x] **4.2** `<VersionHistory>` component on the product detail page — timeline + click-to-preview JSON snapshot dialog + "Restore this version" button. Restore writes the snapshot back, records a fresh `restored` revision, emits a `catalog_changed` webhook so chatbots see it.
- [x] **4.3** ALIGNED admin panel at `/aligned-admin` (gated by `requireAlignedAdmin`): orgs list with search + suspend / reactivate / delete, queue depth (import/sync/webhook), Redis ops/s, per-org member/product/service counts.
- [x] **4.4** Notifications: `Notification` table (per-user or org-wide), `<NotificationsBell>` in the top bar with unread badge + dropdown + mark-all-read. Import worker emits notifications on succeeded/partial/failed.
- [x] **4.5** Sentry wired into api (`lib/sentry.ts`) and worker (`@sentry/node`). No-op when `SENTRY_DSN` is unset.
- [x] **4.6** Worker now exposes `/metrics` on port 9100 with default Node metrics + `worker_jobs_completed_total` / `worker_jobs_failed_total` / `worker_last_job_duration_seconds`.
- [x] **4.7** Integration tests in `apps/api/test/`: auth, tenant-isolation, products CRUD, read API, webhook signature.
- [x] **4.8** Tenant-isolation test asserts org A cannot read org B's products by ID and vice-versa — fails the build if RLS is broken.
- [x] **4.9** k6 load test at `infra/scripts/load-test.js` — 500 rps for 2 min across 5 read endpoints, thresholds `p95<200ms`, `p99<400ms`, `error rate<1%`.
- [x] **4.10** Multi-stage Dockerfiles for api / worker / web (node:20-alpine + tini). `docker-compose.prod.yml` with 2 replicas of api/worker/web behind Caddy.
- [x] **4.11** Caddyfile: HTTPS via Let's Encrypt, HSTS, gzip+zstd, health-check upstreams.
- [x] **4.12** GitHub Actions: `ci.yml` (lint + typecheck + test against ephemeral Postgres + Redis services) and `deploy.yml` (build + push to GHCR with commit SHA tag → SSH deploy → `prisma migrate deploy` → smoke test `/health`).
- [x] **4.13** `infra/scripts/backup.sh` — pg_dump → gzip → age-encrypt → upload to Wasabi → prune > 30 days. Cron example in the runbook.
- [x] **4.14** `docs/RUNBOOK.md`: topology, day-1 bootstrap, deploys, rollback, migrations, restore-from-backup, add-tenant, rotate secrets, common incidents, pilot onboarding checklist.
- [x] **4.15** `pnpm db:seed:pilot` creates three pilot orgs (`pilot-cafe`, `pilot-clinic`, `pilot-store`) each with an admin user, sample product/service/FAQ, and an API key (printed once).

---

## 5. Current Status

> **Update this section at the end of every Claude session.** Use ISO dates. Be specific about what works and what doesn't.

- **2026-07-20 FIX: large-catalog "context + answering" defects on max-plan tenants (Aseer Time, 477 products) — bot said "we don't have X" for real items, "only one size", replied English to Arabic, and truncated long lists.** Root-caused NOT to the model (confirmed Sonnet running via provenance — max plan is fine) but to system-level retrieval/prompt limits in [bot-engine.ts](apps/api/src/lib/bot-engine.ts). Fixes (all deployed): (1) **Retrieval caps too small for big menus** — `SMALL_CATALOG` 40→60, search `TOP_K` 20→40, `BROWSE_CAP` 60→150; added a deterministic `orderBy: [{category:{name}},{name}]` on the product query so the browse slice is category-clustered + stable (was arbitrary DB order). (2) **Size-variants-as-separate-products** — new size-sibling inclusion: after retrieval, for every packed product pull ALL siblings sharing its `baseName()` (exported from [variant-image-collapse.ts](apps/api/src/lib/variant-image-collapse.ts)), bounded 60 — so "how many sizes?" is always answerable (Aseer models عادل الأسطورة / شي ثاني sizes as separate SKUs). (3) **Output-token cap** `maxTokens` 240→560 — long box/menu lists were truncating mid-item. (4) **Language** — strengthened the mirror rule ("answer in the language of THIS message, overrides configured order, don't flip mid-convo") + flipped Aseer Time `BotConfig.languages` en,ar→ar,en (prod data). Bundled with two earlier bot-quality fixes in the same deploy: **variant-image collapse** (size variants share one photo → send one, [variant-image-collapse.ts](apps/api/src/lib/variant-image-collapse.ts), WhatsApp+Messenger) and **Markdown normalize** ([markdown-normalize.ts](apps/api/src/lib/markdown-normalize.ts)) — gpt-4o-mini/Sonnet emit `**bold**`/`##` which WhatsApp shows literally; convert `**`→`*` (WA) / strip (Messenger/IG/voice), wired into the reply-validator pipeline + Messenger send. All api `tsc` clean; pure helpers verified standalone (Docker DB off this session → vitest runs in CI).

- **2026-07-17 FIX: hader.ai marketing CSP was blocking Google Tag Manager / GA4 (applied live on prod + repo [Caddyfile](infra/caddy/Caddyfile) — repo edit NOT yet committed).** Tag Assistant reported CSP violations: `script-src`/`connect-src` only allowed Clarity. Added Google's recommended GTM/GA4 domains — `https://*.googletagmanager.com` to script-src; `https://*.google-analytics.com https://*.analytics.google.com https://*.googletagmanager.com` to connect-src (img-src already `https:`). Patched `/etc/caddy/Caddyfile` on prod in place (backup `Caddyfile.bak.<ts>` created), `caddy validate` OK, reloaded, verified live header via curl. Portal `/app` unaffected (still serves its own Next nonce CSP — the marketing CSP stays scoped to the marketing handle per [[project_caddy_portal_csp]]).

- **2026-07-09 FIX: refund the wallet when a metered WhatsApp message FAILS delivery (deployed `6f09a19` + backfill).** Real money bug. Meta bills on DELIVERY, not the send attempt (failed = $0), but `chargeAtSend` debits the tenant the moment Meta ACCEPTS the send — and the `message_status` webhook's `failed` branch never refunded → tenants over-charged for undelivered messages. Fix: new `refundFailedSend(orgId, recipientId)` in [wallet.ts](apps/api/src/lib/wallet.ts) (symmetric to chargeAtSend: credits `available_micros` back, reverses lifetime counters + broadcast `billing_settled_micros`, writes a `release` ledger row; amount read from the recipient's own `settle` ledger row). Idempotent + race-safe via an atomic `refunded_at` claim (`billed_at IS NOT NULL AND refunded_at IS NULL`) so duplicate `failed` webhooks can't double-refund. Wired into the webhook (whatsapp.routes.ts ~2630): on `failed` for a billed recipient that was NEVER delivered/read, fire-and-forget refund after the tx commits. Migration `20260709120000_broadcast_recipient_refunded_at` (additive `refunded_at`). **Metering is now LIVE (10 wallets metered — the old "0 metered" note is stale).** Backfilled 796 historical over-charges: **lexy refunded $63.68** (wallet 20.16→83.84). **Follow-up (deployed `0ccd341`):** added the requested (a) **30-min auto-refund reaper** — `broadcast-reaper` sweeps every 5 min and refunds any failed+billed+unrefunded+never-delivered recipient older than a 30-min grace (`WALLET_FAILED_REFUND_GRACE_MINUTES`), a safety net for missed/late `failed` webhooks; and (b) **resend-to-failed billing re-arm** — `rerun-failed` now refunds outstanding charges then clears `billed_at`+`refunded_at` so a resend charges fresh + is refundable again (previously `billed_at` stayed set → resend sent unbilled). `refundFailedSend` twin added to the worker wallet. **Residual gap (still not handled):** a message stuck at `sent` forever (never delivered, never `failed`) — Meta charges $0 but no `failed` webhook fires; the reaper only refunds `failed`, not long-stuck `sent`. See [[project_wallet_billing]].

- **2026-07-03 FIX: Arabic voice notes stuck replying in English (deployed, commit `9f89be8`).** Live defect: a tenant on WhatsApp voice replies (ElevenLabs) got English replies to Arabic voice notes. Root cause was NOT TTS (flash_v2_5 auto-detects + speaks Arabic; the bot's TEXT mirrors Arabic input — verified for all voice tenants incl. en-only full-volume). It was **transcription routing** in `resolveInboundMedia` ([whatsapp.routes.ts](apps/api/src/modules/whatsapp/whatsapp.routes.ts):~250): the old heuristic sent a voice note to **Groq Whisper (English-biased, weak on Arabic)** whenever the LAST bot reply had no Arabic codepoints → Arabic mis-transcribed toward English → bot replied English → next Arabic note again routed to Groq = **sticky-English loop.** Fix: if the tenant's `BotConfig.languages` includes `ar`, ALWAYS use `gpt-4o-transcribe` (strong on Arabic, fine on English); Groq only for non-Arabic tenants with a clearly-English last reply. Also set **full-volume** `languages` `en`→`ar,en` (Lebanese shop, was English-only). NOTE for ops: the per-tenant ElevenLabs voice id must be SAVED (BotConfig.ttsVoiceName) — all 4 voice tenants had it empty (using env default voice); model still speaks Arabic but the chosen voice sounds better once saved.

- **2026-07-03 Hader Support bot → SUPPORT + SALES blend + demo booking + test scenarios (data-only, live).** Rewrote the hader-support persona (now 7.5k chars, identity "Hader") into a friendly assistant that does support AND sales (leans sales): pitches Hader, asks about the prospect's business, onboards, and proactively offers a **free demo**. **Demo → booking:** re-enabled the `bookings` feature (disabledFeatures now just `[shopify, orders]`) + set `BusinessInfo.bookingForm` (enabled, fields name/phone required + business/preferred_time optional, `availability:null`). The bot collects name+number and emits `[BOOKING:]` immediately (verified live — marker fires on name+number in one turn) → saved to `/bookings` by the existing capture path. Added 8 sales/discovery FAQs (174 total, embedded 174/174) + **13 manual `BotTestScenario` rows** (sales / demo-booking / support / guardrails) that show in the AI bot builder **Test & ship** section and can be Run (LLM-judge). All via the existing seed `packages/db/scripts/seed-hader-support-bot.ts` — no code change, no migration, no redeploy (bookings/bookingForm/scenarios are all existing deployed features). Guardrails intact (off-scope declined, admin internals refused, Meta-connection still referred). Live-verified: sales pitch, pricing→demo, support steps, off-scope refusal all correct.

- **2026-07-03 Exact cache-aware AI pricing (per-tenant USD now matches the Anthropic bill).** Follow-up to the cache-token undercount fix. MessageProvenance now stores the prompt-cache split (`cache_read_tokens` + `cache_write_tokens`, migration `20260703160000_provenance_cache_tokens`, additive default 0) as subsets of `promptTokens` (which stays TOTAL input). New `costUsdWithCache()` in [ai-pricing.ts](apps/api/src/lib/ai-pricing.ts) prices each bucket at its real rate — Anthropic cache-read ×0.10, cache-write ×1.25 of base input; OpenAI cached-input ×0.50; Groq none — and `tokensToUsd()` now delegates to it (cache args optional, so all old callers/rows price exactly as before). `complete()`/`CompleteResult` carry `cacheReadTokens`/`cacheWriteTokens` from every provider (Anthropic `cache_read_input_tokens`/`cache_creation_input_tokens`; OpenAI `prompt_tokens_details.cached_tokens`); threaded through `buildBotResponse` inputs → `recordProvenance`. All 3 admin cost aggregations (org-detail, system, ai-usage) sum + price the split. **Backfill** (`packages/db/scripts/backfill-provenance-tokens.ts`) recomputes the pre-fix hader-support rows' true input from their stored system-prompt snapshot (the 7 rows that logged 9–228 instead of ~4,200). Note the two OTHER reasons Hader-per-tenant ≠ Anthropic-whole-key remain by design: the dashboard is the whole key (all tenants + any direct/test calls, e.g. build-time verification runs), and Ultra Haiku aux still isn't per-tenant recorded (0 Ultra tenants). api tsc-clean.

- **2026-07-03 AI-cost UNDERCOUNT fix — prompt-cache tokens were never recorded.** Root-caused why the admin AI cost card (`$0.01` / 971 tokens for a Max tenant) is far below the real Anthropic bill. `completeMax`/`completeUltra` ([openai.ts](apps/api/src/lib/openai.ts)) cache the large per-tenant system prompt (`cache_control: ephemeral`) but recorded only `res.usage.input_tokens` — the UNCACHED remainder. Prod proof: hader-support's 7 Sonnet rows logged 9/9/44/77/105/168/228 input tokens (just the user message) instead of ~3,500 (persona+FAQs). Fixed: new `anthropicInputTokens()` sums `input_tokens + cache_read_input_tokens + cache_creation_input_tokens` so MessageProvenance/cost reflect the true billed input. **USD is now a close estimate** (all input priced at the standard rate; cache-reads are really ~10% and cache-writes ~125% — exact split would need cache-token columns + cache-aware pricing = follow-up). Other gaps (not fixed): (a) **Haiku aux calls** (intent + per-contact persona via `completeFast`) hit Anthropic but write NO provenance (0 haiku rows in prod) → uncounted; (b) the Anthropic dashboard is the **whole key** (all Max/Ultra tenants + any direct/test calls, e.g. this session's `buildBotResponse` verification runs on hader-support), while Hader's number is one tenant's final replies. Past rows stay undercounted (not backfilled). api tsc-clean.

- **2026-07-03 Split the `catalog` feature toggle → `products` / `services` / `business_info` / `imports` (4 independent per-tenant toggles).** Replaces the single `catalog` key in [org-features.ts](packages/shared/src/constants/org-features.ts) with four (products→`/products`+`/categories`, services→`/services`, business_info→`/business-info`, imports→`/imports`). Web-only gating (sidebar + dashboard `layout.tsx` route guard via `isHrefDisabled`; no API gate ever existed for catalog, kept that way). Updated the 3 web consumers (`analytics` `hasCatalog` → products||services, `billing` + `usage-limits` `QUOTA_FEATURE` maps → per-key) + the hader-support seed keep-list. Admin Features panel + create-tenant form auto-render from `ORG_FEATURES`, so the 4 toggles appear automatically. **Migration `20260703120000_split_catalog_feature`** (data-only) backfills every org that had `catalog` disabled → all four keys (identical effect); orgs without it untouched (all four enabled). **Never reintroduce a combined `catalog` key** (mirrors the orders/bookings split rule). All 3 packages tsc-clean.

- **2026-07-03 Hader Support = platform-wide TECHNICAL-SUPPORT bot (prod-configured + live-tested; no engine change).** The `hader-support` org (id `b8964704-8ba7-4690-b6fa-9872c9f773f4`) is now Hader's own support assistant for ALL tenants — answers "how do I use Hader / where is button X / fix my problem", escalates to a human on anything it can't solve. Built entirely with existing levers (NO edit to the shared `bot-engine.ts`): `BusinessInfo.legalName='Hader Support'` (bot identity), `BotConfig.adminSystemPromptAppend` = a ~5k-char **support persona + guardrails** (authoritative, injected after core rules), **166 FAQ rows = the knowledge base** (top-K retrieved + embedded 166/166) — core + deep WhatsApp *usage* (24h window, template categories/rejection, quality rating, messaging tiers, opt-in) + bot behaviour + catalog/imports/payments/voice + ~30 **exact button-placement / step-by-step navigation** guides (verified real labels: Deploy on AI bot builder, "Block customer" in the conversation ⋯ menu, New product, New broadcast; full sidebar map). **DELIBERATELY EXCLUDED — WhatsApp/Meta CONNECTION:** per owner directive, the bot NEVER walks a tenant through connecting a number to Meta (credentials/WABA/App ID/Secret/token/verify/subscribe/display-name/business-verification/registration-PIN) — a dedicated persona rule + referral FAQs make it always say "the Hader team sets this up for you — want me to refer you to Hader support?" and offer [HANDOFF]; same for Instagram/Messenger connection. Usage-after-connect help is unaffected. `aiPlan='max'` (Claude Sonnet — follows the reframing reliably), `monthlyAiMessageCap=null` (unlimited), `deployedAt=now`, `disabledFeatures=[shopify,orders,bookings]` (a help desk, not a shop). The persona reframes the shop-flavoured base prompt: no menu/products/cart/images; answer only from the KB + the user's own account; **NEVER reveal admin-only internals** (HQ panel, other tenants, Meta cost/margins/profit/price-floor, infra/secrets/keys, which AI model powers it); **escalate → [HANDOFF]** when stuck / account-specific (billing, refunds, top-ups, bugs, suspensions, security) / user asks for a human. FAQs auto-embedded by the 3-min tick (50/50). Reproducible seed committed at `packages/db/scripts/seed-hader-support-bot.ts` (idempotent; `pnpm --filter @aligned/db exec tsx --conditions=source packages/db/scripts/seed-hader-support-bot.ts`). **Live E2E test (real engine, Claude):** WhatsApp-setup Q → exact 5-cred steps; "bot not replying" → 6-point checklist; "what caused WWII?" → declined+redirect; "profit margin per msg?" → refused (internal); "show me the menu" → "no menu, this is a help desk" + redirect. All correct. Runs on the hader-support WhatsApp number wired earlier today (+961 81 536 569).

- **2026-07-03 Hader Support WhatsApp number wired live (prod DB config, no code change).** The `hader-support` org (id `b8964704-8ba7-4690-b6fa-9872c9f773f4`, channel `0c106ceb-1aa7-4a03-96af-5c81f1626974`) had an EMPTY primary WA channel. User supplied a **cross-app credential mix** (token from app **Demo I** `4478141549176683`; App ID + Secret from app **Demo II** `865801309419691`) which our `assertMetaCredentialsConsistent` guard correctly rejects. Verified via Graph API (`debug_token`, app|secret probe) and standardized on **Demo I**: token (given, valid, whatsapp scopes) + App ID `4478141549176683` + App Secret `8de49101cd8f47fd79e09b5cce33f1a2` (confirmed belongs to 4478). Paired with the new **Demo II WABA `1375568407768555` / phone `1089550014250670` (+961 81 536 569)** — a WABA can be subscribed by any app that has access. Wrote creds through the app's encrypting Prisma client (`withSecretCrypto` → accessToken/appSecret AES-GCM at rest; decrypt round-trip confirmed), set `is_active=true`. Ran `infra/scripts/wa-backfill-subscribe.ts` → registered the WABA override callback `→ https://api.hader.ai/api/v1/whatsapp/webhook/b8964704-…`; Meta `subscribed_apps` now shows Demo I + that callback → **inbound live, HMAC secret matches subscribed app**. No deploy needed (creds read from DB per-request). Note: phone `code_verification_status=EXPIRED` (display-number verification lapsed; doesn't block an already-onboarded Cloud API number). Unrelated pre-existing FAIL surfaced by the backfill: **aseer-time +961 70 547 407** WABA isn't app-subscribed (Meta #100 "app must be subscribed before override") — needs a plain app-subscribe first; left untouched. See [[project_whatsapp_onboarding]].

- **2026-07-01 Tenant WALLET & metered WhatsApp billing (DEPLOYED + prod-verified; commit `50adb5a`; all 5 packages tsc-clean incl. web `tsc --noEmit`).** Per-tenant PREPAID wallet that charges per WhatsApp message sent. See [[project_wallet_billing]]. Spec `docs/wallet-billing-plan.md`, CEO brief `docs/wallet-billing-ceo-brief.pdf`.
    - **Money = integer MICRO-USD (µ$).** Default price **$0.08/msg** (HQ-changeable per tenant; floor $0.0375 = Meta cost). New `packages/shared/src/schemas/wallet.ts` (`formatMicrosUsd`/`usdToMicros`/`microsToUsd` + constants + Zod bodies). New `INSUFFICIENT_BALANCE` (402) code + `paymentRequired()` helper.
    - **Model — CHARGE-AT-SEND (not hold/settle).** Atomic conditional SQL (`UPDATE tenant_wallets SET available -= price WHERE available >= price`) — race-safe, never negative, caps to balance. Engine `apps/api/src/lib/wallet.ts` (owner prisma + explicit org filters) + worker twin `apps/worker/src/lib/wallet.ts`. Idempotent per recipient via `broadcast_recipients.billed_at`. The hold/settle/release fns exist but are unused live → **no reconciliation reaper needed**.
    - **`metering_enabled` gate is the send switch.** A wallet can exist (price set, visible) with metering OFF = sends NOT gated. `topUp` auto-flips it ON; HQ toggles. Backfill (`packages/db/scripts/backfill-wallets.ts`) created wallets for **all 10 prod orgs at $0.08, metering OFF** — so NOTHING breaks until HQ tops up.
    - **Enforcement:** broadcast `/send` rejects upfront (402, rolls back to draft) if a metered tenant can't afford ≥1 msg, then snapshots the accepted price onto the broadcast. Send worker + sequence-tick: pre-send `canAfford` skip (recipient `skipped`/`insufficient_balance` + notify once) + post-send `chargeAtSend`. Test-send gated. Unmetered tenants untouched.
    - **DB** (migrations `20260701125000_wallet_audit_actions` + `20260701130000_tenant_wallet_billing`): `tenant_wallets` (@unique org, available/held µ$, price, metering_enabled, meta cost, lifetime counters) + `wallet_ledger` (topup/adjust/hold/settle/release signed + balance-after) — both RLS (migration + rls.sql; tenant-isolation + rls-drift gates cover them). Broadcast billing snapshot cols + `BroadcastRecipient.billed_at`. 4 wallet AuditActions.
    - **API:** admin `GET/PUT .../aligned-admin/orgs/:id/wallet/{price,topup,adjust,metering,threshold}` + `/wallet/ledger` (admin.routes.ts). Tenant self-service `GET /billing/overview` + `/billing/ledger` (billing.routes.ts) — never leaks Meta cost/margin; tenants can't self-top-up.
    - **Web:** tenant `/billing` page + sidebar (`hideForAdmin`) + dashboard wallet widget (`wallet-balance`, hidden when unmetered / no `whatsapp` feature) + pulsating low-balance banner; admin wallet card + ledger on org detail; broadcast wizard cost line.
    - **Prod verify:** `tenant_wallets`+`wallet_ledger` exist, 10 wallets (0 metered, $0.08 each), audit enum has the 4 values, `/api/v1/billing/*` + admin wallet routes = 401 (registered), `/app/billing` = 200, health OK.
    - **NOT run locally (no Docker DB this session):** `apps/api/test/wallet.test.ts` + the extended tenant-isolation gate — CI runs them. If CI is red, triage those two files first.

- **2026-06-30 (session 2) Per-tenant MONTHLY AI-MESSAGE allowance (DEPLOYED + prod-verified; commit `763ac7e`; all 5 packages tsc-clean; voice+voice-order+tenant-isolation 17/17).** Replaces the global daily TOKEN budget (which bricked Sandwich Wnos's bot when the 200k cap was hit — every reply threw `TOKEN_BUDGET_EXCEEDED` → "Sorry, I didn't quite catch that"). See [[project_ai_token_budget]].
    - **Model:** admin sets `Organization.monthlyAiMessageCap` per tenant (migration `20260630180000`, default **2000**, null=Unlimited; backfilled all 10 prod orgs to 2000; HQ admin org unlimited via `isOrgUnlimited`). **1 AI message = 1 bot reply (chat) or 1 bot turn (voice).** Counter `aimsgs:{org}:{YYYY-MM}` (Redis, monthly). Logic in `apps/api/src/lib/ai-messages.ts`.
    - **Enforcement:** WhatsApp + Messenger/IG gate before the reply (over cap → skip + leave for human) + `recordAiMessages(1)` after; voice counts a call's bot turns on first `/end` + `/voice/config` serves a brief "unavailable" persona when over (voicebot hard-refusal = small Aseer-time follow-up). **No daily cap.**
    - **Daily token budget → COST-ONLY:** `consumeDailyTokens` still tracks (admin token/USD view) but never gates (returns true); removed the token threshold notifications. This kills the outage failure mode.
    - **Alerts:** 80% + 100% notify the tenant AND every ALIGNED admin (deduped/month). Tenant dashboard banner ([ai-budget-banner.tsx](apps/web/src/components/dashboard/ai-budget-banner.tsx)) shows AI MESSAGES (never tokens), **pulsates red from 80%, solid red "paused" at 100%**.
    - **Admin UI:** org detail *AI plan & usage* card — editable number + Unlimited + used/cap (`PUT /aligned-admin/orgs/:id/ai-message-cap`); also on the create-tenant form. ai-usage endpoint returns `aiMessages{used,cap,unlimited,percentUsed}`.

- **2026-06-30 Universal auto-embedding + bot scope-lock (DEPLOYED + live-verified; api `tsc` green; voice 12/12 + tenant-isolation 5/5 green). Commits `e6cddd9` (embeddings) + `ed92e9e` (scope-lock).** See [[project_auto_embedding]] + [[feedback_bot_scope_lock]].
    - **Embeddings — root-cause finding:** the bot's top-K retrieval runs on `embedding`, but only the catalog API routes embedded on write — **CSV/XLSX import, Shopify sync, direct-DB writes did NOT embed**, so imported catalogs were invisible to the ranker (le-gabarit 11/301, booty-republic 2/27, etc.). And **services/FAQs had no embedding column at all**. Worse, `gatherBotData` capped products at `take:30`, so the bot only ever saw 30 of any catalog and the ranker (engages >40) was dead code — after the 477-product Aseer import the bot saw 30.
    - **Fix:** (1) Service + FAQ gain `embedding`/`embeddingHash` (migration `20260630120000_service_faq_embeddings`, additive on existing RLS tables). (2) New **embed-backfill tick** `apps/api/src/lib/embed-backfill-tick.ts` runs **in the API process** (the embedding lib lives in apps/api), every 3 min, embeds any product/service/FAQ with `embedding_hash IS NULL` across EVERY org (batched 96/call, 250/tick) — guarantees everything is embedded no matter how it was added; idempotent so steady-state is free; wrapped so a tick error can't crash boot. (3) Embed-on-write added for service + FAQ create/update (products already had it). (4) Raised `gatherBotData` loads (products 600, services 300, FAQs 300) + added embedding **top-K selection for services + FAQs** in `buildBotResponse` (~15/12; were dumped wholesale). Helpers `serviceEmbedText`/`faqEmbedText`/`embedServiceAndStore`/`embedFaqAndStore` in `embedding.ts`.
    - **Live result after deploy (tick auto-backfilled in ~3 ticks):** prod 100% — **products 864/864, services 61/61, FAQs 80/80** across all 6 tenants. Tick logs clean.
    - **Bot scope-lock (live defect):** Sandwich Wnos bot answered "what caused WWII?" and "1+1?" from the model's training — the prompts had a "don't invent products" rule but NO scope rule. Added a forceful **SCOPE LOCK as rule #1** in `bot-engine.ts` (tied to the business identity line + reinforced as the first Core rule) and `voice-prompt.ts` (first STRICT CALL RULE): answer ONLY from the tenant's data, refuse all general knowledge with a one-sentence decline+redirect, pleasantries still allowed. Applies to every tenant + both conversational models. **Prompt-only for now** — if gpt-4o-mini still leaks, pair with a cheap pre-classifier backstop per [[feedback_bot_engine_directives]] (not built; awaiting live verification by the owner).
    - **Channel-identity fix (commit `77b6a11`, deployed):** bot was calling itself a "WhatsApp assistant" on Instagram. A workflow audit confirmed the `channelLabel` plumbing is already CORRECT (WhatsApp→'WhatsApp' [whatsapp.routes:3834], Instagram→'Instagram' / Messenger→'Facebook Messenger' [messenger.routes:750, `channelKind` from webhook `body.object==='instagram'`]; cart-flow doesn't call buildBotResponse directly). Root cause was prompt-STRENGTH: the identity rule was a soft nudge gpt-4o-mini ignored. Fix: (1) hardened the identity rule in `bot-engine.ts` into a forceful prohibition ("You are a <channel> assistant"; on non-WhatsApp it's STRICTLY FORBIDDEN to say "WhatsApp"); (2) de-hardcoded the voice `deliveryBanner`'s "WhatsApp voice note" → channel-neutral (it planted a WhatsApp identity first in the prompt; latent on IG/Messenger since they reply in text, live on WhatsApp voice).
    - **Cart bricking + LBP overflow fix (commits `f455666` resilience, `1d93476` BigInt; both DEPLOYED + prod-verified — all 5 cart money cols now `bigint`, 47 carts/74 items intact, logs clean; full api suite 129/129).** Live defect: the Sandwich Wnos (LBP) bot got STUCK replying the hardcoded English "Sorry, I didn't quite catch that" to EVERY message — even English — after a large-order attempt. Two defects (12-agent workflow audit): (1) **Int overflow** — cart money cols were 32-bit Int (max 2.1B minor); LBP ×100 overflows at ~179 drinks / ~21.4M LBP total → Postgres "integer out of range" on write. Fixed by migrating `Cart.{subtotal,delivery,total}Minor` + `CartItem.{unitPrice,lineTotal}Minor` → **BigInt** (migration `20260630160000_cart_money_bigint`). (2) **Unguarded draft-cart LOAD** (`cartStateForLLM` in whatsapp.routes + `loadDraftCartState` in cart-flow) threw on the poisoned cart and bricked every later turn → wrapped in try/catch → null. BigInt convention: arithmetic in JS number, `Number()` on read/serialize, `BigInt()`/`toBig` on write; `bigint-json.ts` toJSON shim (api+worker) backstops untyped serialization. Catalog prices stay Int. See [[project_cart_money_bigint]]. **The scope-lock from earlier this session is confirmed working live** (bot declined WWII/1+1/eurostar/BMW-vs-Audi).
    - **Also this session:** hard-deleted the 21 old soft-deleted Aseer Time products (nulled 38 cart_item links first — orders keep their name/sku/price snapshot). 140 older historical soft-deletes left untouched (offered to purge).

- **2026-06-25 Shopify integration (full feature) + 2 fixes + 2 UX tweaks (full-monorepo `tsc` green 7/7; api tests 119/120 — the 1 failure is the SAME pre-existing `webhook-signature` connector test, unrelated).** First real Shopify client onboarded (The Booty Republic, `1tnjz1-wa.myshopify.com`). Built an opt-in, per-tenant **Shopify sync** feature end-to-end: connect → scrape everything → review/approve → import → live-sync. Migration `20260625130000_shopify_integration` (3 tables: `shopify_connections`, `shopify_scrape_runs`, `shopify_staged_items` + 3 enums + 2 NotificationKind values; RLS inline + in rls.sql; applied to dev). See [[project_shopify_onboarding]] for the credential-gathering gotchas.
    - **Feature flag (opt-in):** new `shopify` key in [org-features.ts](packages/shared/src/constants/org-features.ts) with `defaultDisabled: true` + new `ORG_FEATURE_DEFAULT_DISABLED`. New-tenant UI + admin create-org both default it OFF; migration backfills `'shopify'` into every existing org's `disabledFeatures`. An ALIGNED admin enables it per tenant from the features panel (Booty Republic = the only one ON). **Never make it enabled-by-default.**
    - **API** [shopify.routes.ts](apps/api/src/modules/shopify/shopify.routes.ts): GET/PUT (creds encrypted via `encryptJsonSecret`, write-only, masked), POST verify (shop.json via `safeFetch` → registers webhooks + auto-sync cron), POST scrape/import (enqueue `QUEUE_SHOPIFY`), staged list/approve/reject/approve-all, scrape-runs, DELETE. Every route gated by `disabledFeatures.includes('shopify')` → `FEATURE_DISABLED`. Public **webhook receiver** [shopify-webhook.routes.ts](apps/api/src/modules/shopify/shopify-webhook.routes.ts) verifies `X-Shopify-Hmac-Sha256` = base64 HMAC-SHA256(rawBody, **apiSecret** = the `shpss_` client secret).
    - **Worker** [shopify.ts](apps/worker/src/jobs/shopify.ts) + [shopify-client.ts](apps/worker/src/lib/shopify-client.ts): paginated Admin REST client (Link-header cursor, API `2024-10`); `phase:'scrape'` normalizes products(+variants/images)/customers→contacts/shop→business_info+channels/policies/pages→FAQs/locations into `shopify_staged_items` (chunked 200/txn; preserves rejected/imported status); `phase:'commit'` upserts approved rows via `upsertOne` + image fetch + variant replace-set, then `emitWebhookEvent('catalog_changed')` + read-cache invalidate. Scheduled/webhook triggers auto-re-commit already-imported rows (live update, no re-approval). **Services omitted** (Shopify has none).
    - **Web:** `/settings/shopify` (connect + scrape + review-queue counts) and `/settings/shopify/review` (per-section tabs, select-all, approve/reject, import approved). Settings → Integrations Shopify card now links here (gated).
    - **Live-verified** the client against the real store: 27 products (variants/prices/images), policies, 9 pages, 2 locations all paginate. Customers (8,145) deliberately NOT copied to dev (real PII) — full scrape→import E2E needs the running stack + is a knowing step. New `apps/api/test/shopify.test.ts` (masked creds, feature gate 403, approve, webhook HMAC valid/invalid) green; rls-drift auto-covers the 3 tables.
    - **2 real bugs fixed along the way:** (1) `ssrfSafeLookup` in [util-ssrf.ts](packages/shared/src/util-ssrf.ts) ignored undici's `{all:true}` lookup mode → returned a single address where an array was expected → `ERR_INVALID_IP_ADDRESS` broke EVERY `safeFetch` on newer undici (images, connectors, crawl, shopify). Now honours both shapes. (2) `recordCredentialAudit` ([audit.ts](apps/api/src/lib/audit.ts)) passed the integration NAME as `entityId` (a `@db.Uuid` column) → the HQ credential-trail insert silently failed the UUID parse for EVERY integration (messenger/whatsapp/connectors/shopify). Now `entityId: undefined` (name stays in entityType + metadata). **BullMQ jobIds can't contain `:`** — all shopify jobIds use `-`.
    - **2 UX tweaks bundled:** contacts page now `resetQueries(['contacts'])` after add/import so the new contact appears immediately; inbox template bubbles now render the FULL rendered template (header+body+footer) + the template's buttons as pills (was just the name) — [whatsapp.routes.ts](apps/api/src/modules/whatsapp/whatsapp.routes.ts) test-send persistence stamps `rawPayload.quickReplies`.
    - **DEPLOYED + LIVE (2026-06-25, commits `acee2dc`→`9da5912`):** pushed to prod (hader.ai, SSH `aligned@88.80.145.171:269`, `infra/scripts/redeploy.sh` — migration auto-applied, backfill disabled `shopify` for all 10 orgs). Enabled `shopify` for **The Booty Republic** only (org `db6453d5-2a94-4b5c-9d15-c3455bec1146`, slug `the-booty-republic`) by `array_remove`ing it from `disabled_features`. Client connected via `/settings/shopify`, scraped + approved; first **import imported all 27 products (50 images) + 3,439 contacts + 5 FAQs + 1 policy + 2 locations.** Triggered the commit job server-side (BullMQ `shopify` queue, phase `commit`) when the user only approved (approve ≠ import).
    - **2 import bugs found+fixed live during go-live:** (1) `upsertOne` derived product `slug` from name → 15 same-named Shopify bundles collided on `(org,slug)` unique → now resolves a free slug (base, base-2, …) ignoring own SKU ([shared-upsert.ts](apps/worker/src/jobs/shared-upsert.ts)). (2) Shopify oversold inventory is **negative**; `productSchema.stockQuantity` is nonnegative → 7 products failed Zod → normalizer now clamps to null when ≤0 ([shopify.ts](apps/worker/src/jobs/shopify.ts) `normProduct`). After both fixes + re-running commit: 27/27 in. **Gotcha:** staged rows cache the normalized JSON from scrape time, so a code fix needs a re-scrape OR a `jsonb_set` on the staged rows before re-commit.
    - **Still open:** the pre-existing `webhook-signature` connector test needs separate triage. Live Shopify webhook delivery (product edits → auto-update) not yet observed end-to-end on prod.

- **2026-06-25 Voice bot fidelity overhaul (forms/bookings/caller-continuity/FAQ + hardening). DEPLOYED + verified live** (brain `redeploy.sh` on .171 OK, all 3 migrations applied — `carts.channel/call_uuid/phone_integration_id`+`phone_integrations.bot_enabled` present, `api.hader.ai/health` OK; voicebot `alignbot.js`/`audiosocket.js` copied to .146 + `aseer-voicebot` restarted, boot log "voice config refreshed … instructionsChars=8498"; live `/voice/config` returns structured `orderForm` with the org's exact keys `delivery_address`/`payment_method`/`notes`. Backups on .146: `*.bak.20260625-114023`. Remaining: a live test call; owner should populate the empty FAQ/Policy tabs). Triggered by live defects on the Ogero→.146 voicebot for org **Sandwich Wnos**: a phone order saved with the delivery address dumped into NOTES and the configured form fields **(empty)**, no phone bookings, no caller memory, no FAQs. A 12-agent read-only audit (8 investigators → synthesis → critic → 4 verification probes) confirmed those + ~25 more issues. Plan: [radiant-gathering-island.md](../../.claude/plans/radiant-gathering-island.md). All packages `tsc` green; **new `apps/api/test/voice-order.test.ts` 9/9 + existing voice 3/3 + voice-order-match 5/5 + tenant-isolation hard gate 5/5 all PASS** against the dev PG+Redis stack.
    - **Migrations** `20260625130000_voice_fidelity_p0` (Cart.channel/phoneIntegrationId/callUuid, CartItem.needsPricing, VoiceCall.callerPhoneNormalized/contactId, Booking.channel/callUuid, PhoneIntegration.botEnabled — all additive columns on existing RLS tables, no new RLS) + `20260625130500_booking_received_notification` (`ALTER TYPE NotificationKind ADD VALUE 'booking_received'`, separate migration so ADD VALUE runs alone).
    - **ROOT-CAUSE FIX (forms):** the voice order route built a HARDCODED `{address,notes,fulfillment}` fields object; [captureCart](apps/api/src/lib/cart-flow.ts) only keeps answers whose key matches the tenant's configured `shopForm.fields` keys → every answer dropped to `(empty)` + address leaked into a synthetic `cart.notes`. Now the voicebot's `submit_order` carries a `fields` map keyed by the tenant's EXACT field keys (built dynamically from the new structured `/voice/config` `orderForm`), the route passes it straight to captureCart, and **the synthetic note write is deleted** (provenance now lives on `Cart.channel='voice'`+`callUuid`). The "Anything else?" field is the canonical notes field.
    - **New voice capabilities:** phone **bookings** (new `lib/voice-booking.ts` `createVoiceBooking` — threadId-null-safe, reuses `computeOpenSlots`/`resolveSlotFromText`/`slotHasRoom`; new `POST /voice/calls/:uuid/booking`; `submit_booking` tool built from `bookingForm`); **caller continuity** (new `GET /voice/caller-context?phone=` returns name/openOrder/pastOrders, injected into the realtime prompt; `continueExisting` merges into the caller's open cart); Contact auto-upsert on call-start (channel `voice`) + spoken-**STOP** transcript scan on call-end → `optedOutAt` (compliance parity). voice-prompt.ts rewritten field-driven (shop rules: min/fee/free/confirmation/intentKeywords; full product descriptions; operator phrasings via `extractIntents`; spoken-greeting override; services cap; structured `orderForm`/`bookingForm` in the wire config).
    - **Safety:** order **idempotency** (retried submit_order for same callUuid → returns existing, `merged=true`, no dup cart/bill); server-side **min-order + required-field guards** (400 w/ a spoken message, not an unfulfillable order); **withheld/anonymous caller** → `voice_<callUuid8>` placeholder (never empty `customerPhone`, which would collide every withheld caller on Contact upsert); unmatched spoken items → `CartItem.needsPricing=true` at price 0.
    - **P1:** per-line `PhoneIntegration.botEnabled` gate (config + resolve); payment-paid → WhatsApp the **voice caller** a confirmation (no inbox thread) ([confirm.ts](apps/api/src/lib/payments/confirm.ts)); **voice-call reaper** worker (hourly, flips calls stuck `in_progress`>24h to `dropped`); F2 explicit cache-invalidate on FAQ/policy create+update; F3 truncation logging. **DEFERRED (scoped follow-up): S1 voice AI-provenance** (citation/hallucination scan of voice turns + a `VoiceCallProvenance` table + admin UI) — a separable subsystem; the new /voice-calls transcript page already gives operators visibility. SEC1/SEC2 (gateway-secret boot-enforce / resolve throttle) documented-deferred (gateway mode not in use; forcing the secret required would break the live dedicated-mode server).
    - **FAQ Issue (relaxed gate + DATA finding):** voice FAQ gate relaxed to `isPublished`-only (ignores public/private visibility — a call can't render the distinction) via `gatherBotData(tx, orgId, { includePrivateFaqs:true })`. **BUT the prod DB check (psql on .171) showed Sandwich Wnos has `faq_all=0`, `pol_all=0`** — the tabs are simply EMPTY; the owner must add content. The relaxed gate still helps other tenants with private-but-published FAQs.
    - **Web (delegated agent):** `/voice-calls` page (list + transcript viewer) + sidebar entry; `orders-by-channel` dashboard widget + `Cart.channel`/`needsPricing`/`phoneIntegrationId` on the cart serializer + shared schema; per-line **AI bot** toggle on `/phone-integrations`; `needsPricing` badge in `/cart`. (Web build ignores TS, so api `tsc` is the gate — green.)
    - **Voicebot (Aseer-time repo):** `audiosocket.js` builds `submit_order`/`submit_booking` tool params DYNAMICALLY from the config's form fields, injects a CALLER CONTEXT block, DTMF documented as logged-not-forwarded; `alignbot.js` `getCallerContext`/`submitBooking` + discriminated results so a guard 400 (below-min/missing-field) is SPOKEN to collect more, not escalated. `node --check` green. Model confirmed `gpt-realtime`/`alloy`/`gpt-4o-mini-transcribe`.
    - **Deploy:** brain to **.171** (SSH port **269**, user `aligned`) via `redeploy.sh` (runs `prisma migrate deploy` — applies the 2 new voice migrations alongside the shopify one + restarts api/worker); voicebot 3 files to **.146** + `systemctl restart aseer-voicebot`. Deploy brain FIRST (additive/back-compat). **Brain SSH host key** `SHA256:ZMIvmc2wCXgjObd1MfzzAuDdkjsG7+ZlChvizjR2xmU`.

- **2026-06-24 White-box security audit + 6 High fixes (branch `security/high-findings-fixes`; full-monorepo `tsc` green; api tests 113/114 — the 1 failure is the pre-existing `webhook-signature` stale test, NOT caused by this work).** Full report saved at [HADER-SECURITY-AUDIT.html](HADER-SECURITY-AUDIT.html) (0 Critical, 6 High, 10 Medium, 9 Low/Info; 40+ controls verified). The 6 High findings are now fixed:
    - **H-04/05/06 SSRF in workers:** new [apps/worker/src/lib/safe-fetch.ts](apps/worker/src/lib/safe-fetch.ts) (`safeFetch` = IP-pinning dispatcher + MANUAL redirect re-validation per hop, since a literal-IP redirect target skips the dispatcher's DNS lookup; + `assertUrlResolvesPublic` for Playwright). Wired into import-image fetch ([import.ts](apps/worker/src/jobs/import.ts)), crawl thumbnail fetch + per-child-link enqueue guard + pre-`page.goto` DNS resolve-check ([crawl.ts](apps/worker/src/jobs/crawl.ts)). Crawl streaming switched from WHATWG `getReader()` to Node-stream `for await` (undici body).
    - **H-02 TOTP plaintext:** `totpSecret` now `encryptSecret()` on write / `decryptSecret()` on read (2fa.routes.ts:244, auth.service.ts:197, 2fa.routes.ts:298/359). Backfill extended to `users.totp_secret` ([backfill-encrypt-secrets.ts](packages/db/scripts/backfill-encrypt-secrets.ts)). decryptSecret is plaintext-passthrough so it's backfill-safe + inert without `SECRET_ENCRYPTION_KEY`.
    - **H-01 refresh grace window:** `REUSE_GRACE_WINDOW_MS` 10min → **30s** AND **device-bound** (grace only when the replay's User-Agent matches the session's; a session with no recorded UA falls back to time-window so non-browser clients still work). Every grace re-issue now writes a new `refresh_token_grace_reissue` audit action (migration `20260624130000_refresh_grace_audit_action`, `ALTER TYPE ... ADD VALUE`). UX note: a same-device refresh >30s after rotation now logs out — device-binding is the real control, widen the constant if woken-device logouts resurface.
    - **H-03 MyFatoorah webhook:** signing string now keys-sorted-alphabetically (spec-faithful, removes JS-insertion-order forgeability/divergence), rejects empty `Data`, and a Redis `(org,signature)` nonce (`claimWebhookNonce`, 24h TTL, SET NX) blocks replay (MyFatoorah carries no signed timestamp). Stripe/PayPal were already correct.
    - **Test fix:** `two-factor-recovery.test.ts` updated to assert encrypted-at-rest round-trip (was asserting raw plaintext column).
    - **Deploy follow-ups (out of code scope):** on prod — `pnpm db:migrate:deploy` (adds the enum value), then run the secret backfill once `SECRET_ENCRYPTION_KEY` is confirmed: `set -a; . ./.env.production; set +a; pnpm --filter @aligned/db exec tsx scripts/backfill-encrypt-secrets.ts` (now also encrypts existing TOTP secrets). The remaining Medium/Low items (M-01 withRlsBypass org-bind, M-02 atomic lockout counter, M-04 cookie CSRF Origin check, M-05 force COOKIE_SECURE in prod, etc.) are itemised in the HTML report and NOT yet done.

- **2026-06-23 (session 2) Password-reset link fix + per-tenant feature toggles (commits `782cda2`, `112a745`; deployed, health OK).**
    - **Password reset/verify/invite links were broken in prod:** portal runs under Next.js `basePath: '/app'` but the API minted links off the bare origin (`WEB_PUBLIC_URL=https://hader.ai`), so links 404'd. Fixed `WEB_PUBLIC_URL` → `https://hader.ai/app` on the server's `.env.production` (+ restarted api) and in `deploy-remote.sh:81` / `.env.example` so it survives redeploys. Affected ALL API-minted links (reset, verify-email, invite, billing returns).
    - **Fixed user `lexy@aligned-tech.com`:** login was blocked by the email-verification gate (`!emailVerifiedAt`) even after the lock cleared — set `email_verified_at` directly (root cause was the broken verify link above).
    - **Per-tenant feature toggles (ALIGNED-admin `disabledFeatures`):** split the old single `bookings` key (hid both `/bookings` + `/cart`) into **`orders`** (`/cart`) + **`bookings`** (`/bookings`); added **`messenger`** + **`instagram`** channel toggles (gate bot auto-reply in `maybeReplyOnMessenger` by `channelKind` + operator send in inbox reply route + inbox channel-filter dropdown; `hrefs: []` so the shared `/settings/messenger` page is NOT hidden); added **`exports`** toggle gating the 3 self-service `/exports` endpoints. New `ApiErrorCode.FEATURE_DISABLED`.
    - **ALIGNED-admin can export ANY org's data** regardless of the `exports` toggle: new `POST /aligned-admin/orgs/:id/export`, `GET .../exports`, `GET .../exports/:exportId/download` (reuse the data-export BullMQ worker + DataExport rows; admin downloads from the panel, not the tenant-only email link). New "Data export" card on the org detail page.
    - **Tenant creation** now has a **Features** section (`adminCreateTenantBodySchema.disabledFeatures`) to set toggles up front.
    - **Prod backfill:** orgs that had `bookings` disabled (`aligned-tech`, `aligned`, `lexy`) got `orders` appended so both pages stay hidden (the old key hid both). **Never reintroduce** a single combined bookings/orders key.
    - Full-monorepo `tsc` green. No migration (uses existing `Organization.disabledFeatures`). Web build ignores TS/ESLint (`next.config.ts`), so tsc is the gate.

- **2026-06-23 Multi-number WhatsApp (per-number bot deploy + per-number inbox + broadcast number selection).** All 5 packages `tsc` clean; api tests **113/114** (the 1 failure — `webhook-signature.test.ts` connector inbound — is **pre-existing**, confirmed failing identically on the clean tree with changes stashed; unrelated to this work). Tenant-isolation hard gate + rls-drift green. New `apps/api/test/multi-number-whatsapp.test.ts` (2/2) locks the dedup + inbox filter.
    - **DB** (`migration 20260623120000_multi_number_whatsapp`): `WhatsAppChannel.botEnabled`; `WhatsAppThread.whatsAppChannelId` (FK, SetNull); `BroadcastRecipient.whatsAppChannelId`; `Broadcast.channelIds` (uuid[]). **Thread dedup changed**: dropped the old single `(org, customer_phone)` unique (its real index name was `whatsapp_threads_org_phone_uniq`, NOT the Prisma default — migration drops both names) and replaced with TWO partial unique indexes: `(org, phone) WHERE channel_id IS NULL` (messenger/IG/legacy) + `(org, phone, channel_id) WHERE channel_id IS NOT NULL` (multi-number WA). So the same customer gets a separate thread per number. Backfill stamps existing WA threads + broadcast recipients to the org's primary, sets `bot_enabled=true` on primary. Prisma can't express partial-unique upserts → new helper `apps/api/src/lib/wa-thread.ts` (`upsertWaThread`, findFirst→create with P2002 retry); worker has an inline twin in `inbox-consistency.ts`. All 4 WA thread upserts + the 2 worker upserts converted.
    - **Bot routing**: inbound webhook already resolved the channel by `metadata.phone_number_id` (prior session) — now `maybeReplyAsBot(channelId)` replies from THAT number and gates on `deployedAt && isActive && botEnabled` (per-number AI switch).
    - **API**: per-number routes already existed (`GET/POST /whatsapp/numbers`, `:id/promote`, `DELETE :id`) — added `PUT /whatsapp/numbers/:id` (label/creds/botEnabled/isActive) + `channelId` param on `/whatsapp/verify` and `/whatsapp/test-send`; `/whatsapp/send` now routes by optional `threadId` (sends from the thread's number); `serializeChannel` exposes `label/isPrimary/botEnabled`.
    - **Inbox**: thread DTO gains `whatsAppChannelId/Label/Phone`; list accepts `whatsAppChannelId` filter; inbox-screen channel dropdown lists each WA number (value `wa:<id>`), reply passes `threadId`, header shows a "via <number>" badge.
    - **WhatsApp page** (`/whatsapp`): number switcher (pills), Add number, per-number AI-bot toggle, Make-primary, Remove, and a "Number title" (label) field.
    - **Broadcasts**: wizard multi-selects numbers (`channelIds`); fanout assigns `recipient.whatsAppChannelId` round-robin (SQL `row_number() % k`; single-number = fast updateMany); send worker sends from `recipient.whatsAppChannelId`; resend carries it over. **Rate-limit now keyed per `phone_number_id`** (Meta's real limiter) in both api + worker; worker consumes the token AFTER opt-out/window gates so skips don't burn tokens.
    - **Deviation from plan**: per-number bot deploy lives on the `/whatsapp` page toggle (source of truth `botEnabled`), NOT a separate Bot-page multi-select — simpler, matches where numbers are managed.
    - **Also done this session (Part A, already in commit `851d04e`)**: org switcher removed from top bar; status strip "N chats open" + "bot N today" dropped — code-complete, just needs deploy (user was seeing the not-yet-deployed build).
    - **Not yet done (out of code scope)**: run the migration on prod (`pnpm db:migrate`/`migrate deploy`) + run the F-01-style backfill is N/A (handled in-migration); live test with 2 real Meta numbers. The pre-existing `webhook-signature` connector test should be triaged separately.

- **2026-06-22 Security/ops hardening pass (15 audit findings; full-monorepo `tsc` green; api tests 112/112).** Treated as final-product quality (enterprise clients signing now). Each fix verified.
    - **F-01 connector secrets at rest:** `ApiConnector.authConfig` (JSONB) + `webhookSecret` now AES-256-GCM encrypted via new `encryptJsonSecret`/`decryptJsonSecret` ([packages/db/src/secret-crypto.ts](packages/db/src/secret-crypto.ts)); encrypted on write in connector.routes, decrypted in sync worker + inbound HMAC verifier. Backfill extended to whatsapp+messenger+connectors.
    - **F-02 bot hot path on RLS:** WhatsApp `maybeReplyAsBot`, Messenger `maybeReplyOnMessenger`, and shared `cart-flow`/`contact-memory`/`booking-slots` now run under `withTenant` (RLS-on, `aligned_app` role) not `withRlsBypass` — via a tenant-scoped wrapper bound to the local `withRlsBypass` name at each entry (zero call-site churn; explicit org filters kept). Verified every touched table has a bypass-aware `tenant_isolation` policy + grant.
    - **F-03 inbound HMAC over raw bytes:** connector inbound webhook hashes `req.rawBody`, not a JSON re-serialization.
    - **F-04 payment confirmation (NEW):** `carts` + `paymentProvider`/`paymentRef`/`paymentStatus`/`paidAt` (migration `20260622120000_payment_confirmation`). New `lib/payments/confirm.ts` (idempotent `markCartPaid`) + `payment-webhook.routes.ts` signature-verified receivers for Stripe / MyFatoorah / PayPal → order flips to paid, `order_paid` webhook+notification fire. Bot link-mint persists gateway+ref; new write-only creds `stripeWebhookSecret`/`myfatoorahWebhookSecret`/`paypalWebhookId`.
    - **F-05 SSRF rebinding:** new `@aligned/shared/ssrf` + `lib/safe-fetch.ts` — DNS-resolve+IP-pin dispatcher refusing private/loopback/IMDS; wired into connector test-connection + sync worker.
    - **F-06 key fail-loud:** `SECRET_ENCRYPTION_KEY` validated at boot, required in production (32-byte), optional dev/test.
    - **F-08 log redaction:** pino redact extended to all token/secret/credential paths.
    - **F-09 decrypt fails loud:** `decryptSecret` throws `SecretDecryptError` on GCM tamper / missing key (no ciphertext passthrough).
    - **DR fresh-DB deploy:** early idempotent migration `20260421082152_rls_helpers` defines RLS helpers + `aligned_app` role before first use → `prisma migrate deploy` now works on a clean DB (verified by applying all pending migrations to the dev DB).
    - **tsx/dist stale-schema trap:** `@aligned/db`/`@aligned/shared` exports gain a `source` condition; api/worker run `tsx --conditions=source` → import from source, never gitignored dist. Canonical systemd units added at [infra/systemd/](infra/systemd/).
    - **Read-cache invalidation** added to all catalog sub-resource writes (variants/images/bulk/pricing/availability/category CRUD/locations/contacts/faq+policy deletes).
    - **Email worker** wired ([apps/worker/src/jobs/email.ts](apps/worker/src/jobs/email.ts)) — drains the previously-orphaned `email` queue.
    - **GitHub Actions:** auto-deploy-on-push disabled (account-level blocker documented in deploy.yml) → manual `redeploy.sh`; CI gains dummy WASABI + SECRET_ENCRYPTION_KEY env; embedding-backfill fixed to run natively (was `docker compose exec`).
    - **Dead Docker prod stack removed** (`docker-compose.prod.yml` + Dockerfiles; contradicted native systemd deploy); RUNBOOK banner added; dev compose kept.
    - **Messenger/IG booking** confirmed ALREADY fully implemented (availability + open-slots + `[BOOKING:]` capture) — the older "deferred" note below is stale.
    - **Stale auth test fixed** for the multi-tab refresh grace window (immediate replay→200, post-window→401).
    - **Still needs live verification (out of code scope):** rehearse fresh-DB restore-from-backup; run payment webhooks against real Stripe/MyFatoorah/PayPal sandboxes; install new systemd units on the server; run the F-01 backfill once `SECRET_ENCRYPTION_KEY` is confirmed in prod. UI/UX overhaul is the agreed next phase.

- **2026-06-17 Commerce ported to Messenger/Instagram (commit `0c44d52`, deployed):** The WhatsApp cart/order flow now runs on Facebook Messenger + Instagram DMs without touching the WhatsApp hot path. New `apps/api/src/lib/cart-flow.ts` is a channel-agnostic cart engine that REUSES the WhatsApp `cart-parser` verbatim: `loadDraftCartState` (running totals into the prompt), `syncDraftFromReply` (parse "added" lines → per-thread draft Cart), `captureCart` (promote draft → order `'new'` on the `[CART:]` marker, with marker-payload fallback). Wired into `maybeReplyOnMessenger`: cartState passed to `buildBotResponse`; on `[CART:]` a deterministic receipt is sent and the LLM's own confirmation text is SUPPRESSED (mirrors the WhatsApp double-text fix), thread flipped to `pending`; `[PAYMENT_LINK]` resolved via the tenant's payment provider (`resolvePaymentLink` → MyFatoorah fallback); product `[IMAGE: sku]` already supported (skipped on confirm turns). **Compliance:** each Messenger/IG inbound auto-upserts a Contact keyed by PSID-in-`phoneE164` (numeric, no `+`, can't collide with real +E.164) so the operator block button + `/contacts` work on these channels too; multi-locale STOP/UNSUBSCRIBE sets `optedOutAt`; bot stays silent for blocked/opted-out contacts (inbound still stored). Code-only change (no migration). `tsc` green; cart-parser 11/11. Messenger stays inert until a Page is configured, so WhatsApp is unaffected. **Server SSH password rotated** to `@ligned$$@@@` (two `$`); deploy via `bash infra/scripts/redeploy.sh` succeeded, `/health` OK. **Deferred:** booking capture on Messenger; Meta App Review for messaging permissions (user's side) before real traffic.

- **2026-06-11 Voice media gateway (Aseer-time voicebot integration):** New `/api/v1/voice/*` module connecting the Aseer-time phone voicebot (separate repo, `C:\Users\user\Documents\GitHub\Aseer-time`) to this platform as brain + system of record.
    - **API-key half** (X-Aligned-Api-Key, new scopes `voice:config` + `voice:calls` added to `apiKeyScopes` — the /api-keys page picks them up automatically): `GET /voice/config` returns a compiled realtime-model system prompt + greeting built by the new `lib/voice-prompt.ts` from `gatherBotData()` (persona, hours, locations, contacts, products ≤60, FAQs ≤30, policies) — phone-call style rules (≤25 words, no lists/emoji/markers, `transfer_to_human` escalation), cached in the read-API Redis keyspace (so catalog writes invalidate it). `POST /voice/calls`, `POST /voice/calls/:callUuid/turns`, `POST /voice/calls/:callUuid/end` ingest call lifecycle + finalized transcripts; all writes are idempotent and tolerate out-of-order arrival (turns/end before start auto-create the call row).
    - **Portal half** (JWT, viewer role): `GET /voice/calls` (list, turn counts) + `GET /voice/calls/:id` (full transcript). No web UI page yet — API only.
    - **DB:** `voice_calls` + `voice_call_turns` (migration `20260611130000_voice_calls`, RLS inline in the migration AND in rls.sql; `VoiceCallOutcome` enum: in_progress/completed/handoff/dropped). Prisma client regenerated; full-monorepo `pnpm typecheck` green; `apps/api/test/voice.test.ts` (scope gating, idempotent ingestion, tenant isolation, late-arrival materialization) **3/3 green** against the dev PG+Redis stack; tenant-isolation hard gate 4/4 green.
    - Full api suite: **94/95** — the one failure is pre-existing: `auth.test.ts` "replaying the rotated refresh token revokes the session" expects 401 but the multi-tab fix (`cca77bb`, previous-token grace window) now intentionally returns 200; the test wasn't updated with the behaviour change. Update or delete that assertion.
    - **Bugs found this session (pre-existing):** (1) `prisma migrate deploy` FAILS on a fresh database — migrations from `20260427180000_data_exports` onward call `_aligned_apply_tenant_rls()` / `rls_bypassed()`, but those helpers are only created by rls.sql which runs AFTER migrations. Existing servers don't notice (helpers already present). Workaround used: apply rls.sql lines 28–75 (helpers + role) manually, `prisma migrate resolve --rolled-back <failed>`, re-deploy. Proper fix: add the helper definitions to a migration that precedes their first use. (2) The api test suite requires `WASABI_ACCESS_KEY_ID`/`WASABI_SECRET_ACCESS_KEY` set (dummy values fine) or the 3 import tests 400 on the `isStorageConfigured()` gate — CI masks this because `pnpm test` is continue-on-error. (3) This dev machine has native Windows Postgres on :5432 and Redis on :6379 shadowing the containers — local-only `docker-compose.override.yml` (now gitignored) remaps to 15432/16379, `.env` updated to match.
    - **Voicebot side** (Aseer-time repo): `alignbot.js` client — config fetch with 60s stale-while-revalidate cache + 1.5s cold-cache bound (falls back to static persona), and a bounded (200) serialized fire-and-forget queue for lifecycle/turns so nothing ever blocks the 20ms audio loop. Wired into `server.js` (env `ALIGNBOT_API_URL`/`ALIGNBOT_API_KEY`; captures `callerid`/`exten` from the cloud dialplan UserEvent) and `audiosocket.js` (per-tenant instructions at session start, transcript posting, outcome on close).
    - **To go live:** `pnpm db:migrate` (then rls auto-applies), deploy api, issue an API key with the two voice scopes for the tenant org, set the two env vars on the voicebot server (88.80.145.146), restart `aseer-voicebot`. Run the voice tests once the dev stack is up.
- **2026-05-22 Phase 8 — AI message provenance / audit trail (Phases 1.1 → 1.4):** Every outbound bot reply now writes a provenance row capturing the exact system prompt (SHA-256-deduped via `system_prompt_snapshots`), user message, conversation history, candidate KB rows packed, model + tokens + latency, plus a post-LLM scanner's **citations** (linking each cited product/service/FAQ/business-info field back to its source row with a confidence and a snippet) and **hallucinations** (product-or-price-shaped phrases that don't match the candidate catalog, with severity + reason). Surfaces:
    - `/inbox` — ALIGNED-admin only: click any bot bubble → 4-tab inline panel (Sources / Hallucinations / LLM call / Raw I/O). Flagged bubbles get a rose ring + counter; thread-list rows with flagged replies get a red dot.
    - `/aligned-admin/provenance` — cross-tenant browser with filters (tenant, flagged-only toggle, date range), cursor pagination, inline detail expansion, "View thread →" deep link to `/inbox?thread=<uuid>`.
    - Daily-digest worker (`provenance-digest-tick`) — 24h aggregation of flagged replies per tenant, single email per ALIGNED admin (silent on zero-flag days), Redis distributed-lock.
    - **Critical safety fix bundled:** every literal fictional product name (Karak Tea / Oreo Milkshake / Dubai Crepe / Crepe Pillow / ATK-* SKUs) scrubbed from `bot-engine.ts` system-prompt example blocks; all 17+ refs now placeholders (`<PRODUCT_NAME_FROM_CATALOG>` etc.). Per [[feedback_bot_engine_directives]] / [[no-fictional-product-names-in-prompt]], gpt-4o-mini copies literal examples into real replies even when told not to.
    - Migration `20260522120000_message_provenance` (2 tables + RLS). Commits: `e39e2fc` (1.1+1.2 + scrub), `2ede19f` (1.3 inbox panel), `413afe1` (1.4 cross-tenant + digest), `335bbea` (diagnostic to surface a production-only wiring bug — no provenance rows being written despite code path looking correct; manual probe + tsx import both succeed; diagnostic deployed to whatsapp.routes to capture gate values on the next live bot reply).
- **2026-04-21 QA harness session:** Built `apps/e2e/` Playwright harness (13 specs), 7 `.claude/agents/` QA subagent definitions, root `pnpm qa` / `pnpm qa:gate` scripts, and `.github/workflows/e2e.yml`. First live run surfaced a **critical RLS bypass** (Prisma connected as Postgres superuser, which silently skipped every tenant policy; now `withTenant` switches to the `aligned_app` role). Also fixed: scheduled-sync `__pending__` sentinel never writing SyncRun rows; `connectors.endpoint_url` NOT NULL blocking webhook-only connectors (migration `20260421160000_connector_endpoint_url_nullable`); auth rate limit now env-driven + bypasses for `x-e2e-run: 1` header in dev; read-API exposes `x-cache: HIT|STALE|MISS`; `RATE_LIMIT_READ_API_PER_SECOND` now actually wired in. Tenant-isolation **hard deploy gate** is 6/6 green. Full QA notes: [apps/e2e/QA-REPORT.md](apps/e2e/QA-REPORT.md).
- **2026-05-07 Phase 5 — Hardening + completion:** Shipped 9 sub-phases (5.1–5.9) over a single session. Migrations: `20260507105400_phase4_broadcasts`, `20260507130000_phase5_3_compliance`, `20260507150000_phase5_4_sequences`, `20260507170000_phase5_5_2fa`, `20260507190000_phase5_9_dunning`.
  - 5.1: Fixed broadcast pause/resume bug (paused jobs now self-retry via BullMQ DelayedError instead of being silently skipped). Wired broadcast_started/completed/failed/recipient_failed webhooks. Cancel now removes the delayed BullMQ fanout job.
  - 5.2: Re-run failed recipients endpoint + button. Recipients CSV export. SSE live-counters on the detail page (replaces 3s polling). 50K manual-audience soft cap. Playwright spec for the broadcasts happy path.
  - 5.3: Contacts gain `optedInAt`, `optedOutAt`, `timezone`. Inbound STOP/UNSUBSCRIBE keywords (multi-locale) auto-set `optedOutAt`. Send worker skips opted-out + enforces send-window in the recipient's TZ via Intl.DateTimeFormat. Post-hoc A/B winner determination on completion.
  - 5.4: Drip / sequence campaigns. New `Sequence` + `SequenceStep` + `SequenceEnrollment` tables. Hourly tick worker fires due steps (Redis distributed lock). API + UI shipped.
  - 5.5: TOTP 2FA from scratch (apps/api/src/lib/totp.ts implements RFC 6238 + base32 + recovery codes; no library dep). Login flow gates on totpEnabled. Self-service endpoints + UI in /settings/profile. CSRF + API-key bcrypt skipped by design (documented).
  - 5.6: UI: 2FA settings card, /sequences page (create + enroll dialog), /categories management page, member list pagination + search.
  - 5.7: Read-cache gzipped for payloads >2KB (~70-80% memory savings). Sync worker `__pending__` magic-string sentinel replaced with proper nullable `syncRunId`.
  - 5.8: Bot runtime end-to-end was already wired in `maybeReplyAsBot` (whatsapp.routes.ts). Inbound message → bot-engine → KB-grounded reply → Meta /messages → persisted as outbound. Verified, no code changes needed.
  - 5.9: Hourly dunning tick auto-suspends orgs whose subscription has been past_due for > 7 days (env: `DUNNING_GRACE_DAYS`). New notification kind `org_suspended_for_billing` pinged to all org admins. Deeper analytics (funnels, cohorts, broadcast revenue attribution) deferred — substantial new dashboards.
- **2026-05-07 Phase 4 — Broadcasts:** Persistent Contacts CRUD + Segments (filter AST evaluator) + Broadcasts (CSV/segment/manual audiences, A/B test, scheduled send, per-recipient delivery tracking, pause/resume/cancel, auto-pause on failure burst). New tables: `contacts`, `contact_tags`, `segments`, `broadcasts`, `broadcast_recipients`, `broadcast_events` — all RLS-isolated. New BullMQ queues: `broadcast-fanout` + `broadcast-send` honoring the existing per-org WhatsApp token bucket. Patched the `message_status` webhook to bump recipient state + campaign counters. New web pages: `/contacts`, `/segments`, `/broadcasts`, `/broadcasts/new` (4-step wizard), `/broadcasts/[id]` (live counters + recipient table + timeline). Tenant-isolation hard gate extended to cover contacts + segments. Migration name: `20260507105400_phase4_broadcasts`. New runbook section: "Operating broadcasts".
- **Last session:** 2026-04-22 — Phase 1 (Day 4) shipped. Phase 2 (AI bot builder) + Phase 3 (billing/branding/Meta stepper) shipped between then and 2026-05-07.
- **Current day of plan:** Phase 4 ✅ — **Broadcasts complete.**
- **Next concrete action (deployment session):**
  1. `pnpm install` — picks up `@sentry/node` in api+worker, `prom-client` in worker, plus testing libs.
  2. `pnpm db:migrate` — adds 2 new tables (`catalog_revisions`, `notifications`) + Day 4 enums. Migration name: `day_4_versioning_notifications`. RLS auto-applies.
  3. Run integration tests locally to confirm green: `pnpm --filter @aligned/api test`. **The `tenant-isolation.test.ts` is the hard deploy gate** — do not ship if it fails.
  4. Pre-deploy on the Aligned Cloud Server: copy `.env.production.example` → `.env.production`, fill all secrets, `chmod 600 .env.production`. Configure DNS for `app.aligned.com` and `api.aligned.com` to point at the server. Set GitHub secrets `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_SSH_KEY`, `GHCR_PAT`, `API_DOMAIN`.
  5. Push to `main` — `deploy.yml` builds, pushes to GHCR, SSHes, runs migrations, smokes `/health`.
  6. After deploy: `pnpm --filter @aligned/db exec tsx ./seed/pilot.ts` on the server to create the three pilot tenants. Capture and securely store the printed API keys.
  7. Run the load test from a machine outside the server: `API_KEY=… BASE_URL=https://api.aligned.com k6 run infra/scripts/load-test.js` — must show `p95<200ms`.
  8. Set up the daily backup cron per `docs/RUNBOOK.md` (5 3 * * *).
- **Blockers (these are the only things stopping deployment):**
  - Wasabi access keys (image uploads + CSV imports rely on this)
  - Aligned Cloud Server SSH user + host + domain (`app.aligned.com`, `api.aligned.com` recommended)
  - Resend API key for production email sending (dev works via Mailhog)
  - GitHub secrets configured: `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_SSH_KEY`, `GHCR_PAT`, `API_DOMAIN`
  - **(Phase 2 only — not blocking Phase 1)** Meta WhatsApp Business API credentials.
- **What works (code-complete; not yet run end-to-end):**
  - **NEW (Day 4):** Versioning — every product/service/business-info create/update/delete writes a `CatalogRevision` with the full snapshot. UI: timeline + JSON preview + restore button on the product page. Restore writes the snapshot back, records a `restored` revision, and emits a `catalog_changed` webhook.
  - **NEW (Day 4):** Notifications — `Notification` table + bell icon dropdown in the top bar with unread badge + mark-all-read. Import worker emits succeeded/partial/failed notifications.
  - **NEW (Day 4):** ALIGNED admin panel at `/aligned-admin` — orgs list (search, suspend, reactivate, delete), system health (queue depth, Redis ops/s, user counts, org status counts).
  - **NEW (Day 4):** Sentry on api + worker (no-op when DSN unset). Worker exposes `/metrics` on :9100.
  - **NEW (Day 4):** Integration tests (Vitest + Fastify inject): auth, **tenant isolation (HARD GATE)**, products CRUD, read API, webhook signature.
  - **NEW (Day 4):** k6 load test asserting `p95<200ms` at 500 rps on the chatbot read API.
  - **NEW (Day 4):** Multi-stage Dockerfiles for api/worker/web. `docker-compose.prod.yml` with 2 replicas behind Caddy auto-TLS.
  - **NEW (Day 4):** GitHub Actions: `ci.yml` (lint+typecheck+test against ephemeral PG+Redis) and `deploy.yml` (build + push GHCR → SSH → migrate → smoke test).
  - **NEW (Day 4):** Daily Postgres backup script (`infra/scripts/backup.sh`) → encrypted (age) → Wasabi → 30-day retention.
  - **NEW (Day 4):** `docs/RUNBOOK.md` covers deploy, rollback, restore, add-tenant, rotate-secrets, and incident playbooks.
  - **NEW (Day 4):** `pnpm db:seed:pilot` creates three pilot orgs with sample data + API keys.
  - **NEW (Day 3):** Streaming CSV/XLSX import end-to-end — multipart upload → Wasabi → BullMQ worker → row-by-row Zod validation → tenant-scoped upsert → per-row result rows → live progress in the UI. Imports support products, services, FAQs, business info; categorySlug auto-creates categories.
  - **NEW (Day 3):** API connectors (REST pull + inbound webhook push) with auth-kind selector (none/bearer/api-key/basic/hmac), cron-scheduled syncs via BullMQ repeatables, manual "run now", and an HMAC-verified inbound receiver at `POST /api/v1/webhooks/inbound/:connectorId`.
  - **NEW (Day 3):** Chatbot read API at `/api/v1/read/{products,services,business-info,faqs,policies,search}` — API-key authed (X-Aligned-Api-Key), per-key rate-limited, Redis-cached (60s fresh / 5min stale), invalidated on every catalog write.
  - **NEW (Day 3):** Outbound webhook system — endpoints with subscribed event kinds, HMAC-SHA256 signing (`X-Aligned-Signature: sha256=…`), exponential backoff (8 attempts), permanent-fail short-circuit on 4xx, auto-disable after 25 consecutive failures, manual retry button.
  - **NEW (Day 3):** API key issuance (`/api-keys` page) — secret displayed once, hashed at rest (sha256), scope-checked per endpoint, lastUsedAt tracking + first-use audit.
  - **NEW (Day 3):** UI pages: `/imports`, `/imports/[id]`, `/connectors`, `/api-keys`, `/webhooks`. Sidebar updated to surface them.
  - **NEW (Day 3):** Filtered chatbot Swagger UI at `/docs/chatbot`.
  - **NEW (Day 2):** Full catalog data model — products (variants, images, categories), services (pricing tiers, weekly availability), business info (hours, locations, contacts, FAQs, policies), assets (Wasabi-backed). All tables tenant-isolated via RLS + pg_trgm full-text search indexes + auto-maintained `search_text` triggers.
  - **NEW (Day 2):** All CRUD APIs at `/api/v1/{products,services,categories,business-info,assets}` with cursor pagination, search, filters, bulk update, replace-set semantics for variants/tiers/availability windows.
  - **NEW (Day 2):** Two-step Wasabi upload (presign → browser PUT → finalize). Returns signed GET URLs when bucket is private.
  - **NEW (Day 2):** Product list page (search/filter/bulk-select), product edit page (auto-save details + image gallery + variant matrix), service list + edit (pricing tiers + weekly availability grid), business info editor (5 tabs: profile/hours, locations, contacts, FAQs, policies).
  - Multi-tenant data model with Postgres RLS policies (organizations, users, memberships, sessions, invitations, api_keys, audit_logs)
  - Fastify API at `/api/v1/*` with: health, metrics (Prometheus), OpenAPI docs at `/docs`, helmet, CORS, rate-limit (Redis-backed), cookie support, Zod validation
  - Auth: signup, login, refresh (rotating), logout, verify-email, forgot/reset password, invitations (create + accept + revoke), switch-org, session context
  - RBAC: `requireAuth`, `requireRole('admin'|'editor'|'viewer')`, `requireAlignedAdmin`
  - Tenant context: `withTenant(orgId, fn)` wraps every authenticated query in a Postgres transaction with `SET LOCAL app.current_org_id`; RLS enforces isolation
  - Email templates: verify, reset, invite (Mailhog in dev, Resend in prod)
  - Audit log on every meaningful state change
  - Account lockout after 5 failed login attempts (15 min)
  - Sessions revoked on password change
  - Next.js 15 portal: brand-themed (Mediterranean Blue + IBM Plex Sans), responsive shell with sidebar (desktop) + drawer (mobile)
  - Pages: `/login`, `/signup`, `/forgot-password`, `/reset-password`, `/verify-email`, `/invite/[token]`, `/dashboard`, `/members`, `/settings` + stub pages for `/products`, `/services`, `/business-info`
  - Members page: live role changes, deactivate (with last-admin protection), invite modal, pending invitations list with revoke
  - Org switcher + user menu in top bar
  - Dev stack: docker-compose with postgres, pgbouncer, redis, mailhog
  - Seed script: demo org `demo` + admin `admin@aligned.local` / `Aligned123!`
- **What is NOT yet verified at runtime (user to do):**
  1. `pnpm install` — picks up `@fastify/multipart`, `bullmq`, `csv-parse`, `exceljs`, `undici` in api+worker
  2. `pnpm db:migrate` — Day 3 adds 6 new tables; pick name `day_3_imports_connectors_webhooks`
  3. `pnpm dev` — worker boots all three workers (import, sync, webhook-delivery); api on :4000
  4. Issue an API key in the portal → curl `/api/v1/read/products` with `X-Aligned-Api-Key`
  5. Upload a CSV via the import wizard → watch progress on `/imports/:id`
  6. Add a webhook endpoint pointing at webhook.site → edit a product → see signed delivery
  7. Add a connector with cron `*/5 * * * *` and an endpoint that returns JSON → see scheduled SyncRun appear
- **Known tech debt / deferred to later days:**
  - Member endpoints don't yet support cursor-based pagination (returns full list). Fine for Day 1, will add when org sizes warrant.
  - Audit log has no UI yet (Day 4 admin panel surfaces it).
  - `change-password` endpoint not built yet (only forgot/reset). Add on Day 4 when settings/profile is wired.
  - No CSRF token on top of httpOnly cookies. SameSite=Lax + Bearer-token Authorization header on all state-changing routes mitigates; revisit on Day 4.
  - No 2FA. Out of scope for Phase 1.
  - The `ApiKey` model exists but issuance UI + read-API key auth lands on Day 3.
  - **(Day 2)** Description fields use plain `<textarea>` for now; Tiptap rich-text editor was deferred to keep Day 2 in scope. Markdown-style content is fine for chatbot consumption. Upgrade after Day 4 if needed.
  - **(Day 2)** No category management UI yet — use the API directly or wait for a small page on Day 4.
  - **(Day 2)** Product image reorder uses click "Make primary" only; full drag-reorder UI deferred.
  - **(Day 2)** Variant editor saves only on the explicit "Save variants" button (not auto-saved) because partial variant edits are easy to mistake.
  - **(Day 2)** Pricing-tier edits to existing tiers reuse temporary IDs; on save, the API replaces by the `tiers` array (set semantics) — works but a future cleanup could keep stable IDs across the round-trip.
  - **(Day 3)** Column mapping UI step in the import wizard was deferred — for now the CSV headers must match the template field names. Mapping is supported in the API (`columnMapping` on POST /imports), so a quick wizard step can be added later without backend changes.
  - **(Day 3)** Sync worker's BullMQ-repeatable scheduling uses a placeholder `syncRunId` in the queue payload; the worker creates the actual `SyncRun` row at job time. Slightly hacky — consider switching to QueueScheduler-driven SyncRun pre-creation in Day 4.
  - **(Day 3)** API keys are hashed with sha256 (not bcrypt) for fast lookup. Trade-off: full secret leak from DB → grant access. Keys are random 24 bytes (192 bits), so brute-forcing the hash is infeasible. Acceptable for v1; revisit if any compliance pushback.
  - **(Day 3)** Email queue is created but the email worker isn't wired up; the auth service still calls Resend/Mailhog directly. Not blocking; Day 4 can move email sending to the worker if needed.
  - **(Day 3)** Read cache uses `JSON.stringify` for storage. Fine at <1 MB payloads; if a bot org gets 10K-product responses we should consider compression or pagination keys.

---

## 6. Resume Checklist (read this first each new session)

1. Run `git status` in `/Users/tayseerlaz/Projects/Alignbot` — see what's tracked.
2. Read `## 5. Current Status` above — pick up from "Next concrete action."
3. Check off completed tasks in `## 4. The 4-Day Plan` by changing `[ ]` → `[x]`.
4. If a day ran long, carry incomplete tasks forward — don't silently drop scope.
5. After the session, update `## 5. Current Status` with:
   - Date
   - What was completed (task IDs)
   - What's next
   - Any new blockers or decisions made
6. If the user made a new design decision mid-session, add it to `## 2. Locked Decisions` so future-you doesn't re-open it.
7. Never re-design what's already locked — if something is wrong, flag it to the user, don't silently change course.

---

## 7. Operating Principles

- **Ship vertical slices.** Within a day, prefer finishing one feature end-to-end (API + UI + test) over starting three.
- **Tenant isolation is non-negotiable.** Every new tenant-scoped table gets RLS on the same migration it's created in. Never merge a model without `organization_id`.
- **Zod schemas in `packages/shared` are the single source of truth** for request/response shapes. API validates with them; web imports them for forms.
- **No mocks in integration tests** — use a test Postgres + Redis via Docker. Mocks hide tenant-leakage bugs.
- **Prefer Prisma migrations over `db push`** even in dev, so migration history matches prod from day one.
- **Don't skip the load test on Day 4.** The spec commits to <200ms p95; that's a hard success criterion.
- **Keep `.env.example` in sync** — if you add a new env var in code, add it to `.env.example` in the same commit.
